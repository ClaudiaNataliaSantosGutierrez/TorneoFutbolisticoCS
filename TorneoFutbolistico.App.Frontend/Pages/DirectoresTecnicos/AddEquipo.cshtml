@page
@model TorneoFutbolistico.App.Frontend.Pages.DirectoresTecnicos.AddEquipoModel
@{
}
<h1>Asignar Equipo</h1>
<form method="post">
    <div class="form-group">
        <label for="idDirectorTecnico">Director Tecnico</label>
        <input type="text" id="idDirectorTecnico" name="idDirectorTecnico" class="form-control" value="@Model.directorTecnico.id" readonly>
    </div>
    <div class="form-group">
        <label for="idEquipo">Equipo</label>
        <select name="idEquipo" id="idEquipo" class="form-control">
            @foreach (var equipo in Model.equipos)
            {
                <option value="@equipo.Id">@equipo.NombreEquipo</option>
            }
        </select>
    </div>
    <button type="submit" class="btn btn-primary">Asignar Equipo</button>
</form>
<a class="btn btn-secondary" asp-page="./Index">Volver</a>